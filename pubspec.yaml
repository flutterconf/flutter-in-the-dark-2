name: desafio_flutter_in_the_dark_2
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0
  freezed_annotation: ^0.14.1
  hooks_riverpod: ^0.14.0+3
  layout: ^0.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.3
  freezed: ^0.14.1+3


flutter:
  
  uses-material-design: true

  assets:
    - assets/images/
